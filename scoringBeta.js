/*
//not connected to the bot for now
const swearjar = require('swearjar');

//const filter = new Filter();

//const words = require("./extraWords.json");
//filter.addWords(/*add new shit if u want) 

let messageString = "fuck china"; //for now



isBad(messageString)

export function isBad(messageString) {
    if (swearjar.profane(messageString) && messageString.includes("china")) {
        //score the bitch
        console.log(messageString)
        console.log("gulag")
    }else{
        console.log(messageString)
        console.log("we good")
    }
} 
*/